/**
 * Create an addToCart function that receives an HTMLElement that corresponds
 * to the link that was clicked on the product description. Use that to add
 * a new row to the shopping cart with the correct information.
 * 
 * @param {HTMLElement} link the link that was clicked to add the new row
 */


/**
 * Create a function called moveRowUp that receives an icon DOM element
 * and moves the row that that icon is in up one. Moving the top most row up
 * is not allowed.
 * 
 * @param {HTMLElement} clickedIcon the icon DOM that was clicked
 */


/**
 * Create a function called moveRowDown that receives an icon DOM element
 * and moves the row that the icon is in down one. Moving the last
 * row down is not allowed.
 * 
 * @param {HTMLElement} clickedIcon the icon DOM that was clicked
 */


/**
 * Create a function called updateQuantity() that receives a form element
 * and sets a new price in that row's Price column. Since you will have the
 * form that was submitted, you will need to traverse through the DOM to
 * get to the other elements you need.
 * 
 * If the quantity has been set to zero or a negative number, remove the row.
 * 
 * NOTE: any attribute on an element that starts with data- will be saved
 * into the element's dataset property. Use that to get the price of the
 * item from the form element.
 * 
 * @param {HTMLElement} form the form that was activated that contains the
 *                           quantity field and update quantity button
 */


/**
 * Create a function called updateTotal() that gets all the price totals from all
 * the rows in the table and adds them together, saving them into the span.grand-total
 * element on the table.
 */

